server:
  dataStorage:
    enabled: true
    existingClaim: vault-data-pvc 
    
  auditStorage:
    enabled: true
    existingClaim: vault-audit-pvc
ui:
  enabled: true
  serviceType: LoadBalancer
  externalPort: 8200