apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: resume-storage
  namespace: resume
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
      # storageclass: longhorn #Default
