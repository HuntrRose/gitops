apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: resume-storage
  namespace: resume
spec:
  accessModes:
    - ReadWriteOnce
  resource:
    requests:
      storage: 2Gi
      # storageclass: longhorn #Default
