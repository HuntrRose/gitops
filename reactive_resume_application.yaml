apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: reactive-resume
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://github.com/huntrrose/gitops.git
      targetRevision: HEAD
      path: reactive_resume
  destination:
    server: https://kubernetes.default.svc
    namespace: resume 
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

